import { createOption } from "./utils/option";

export default class Body {
  constructor(body, { size = 0 } = {}) {
    let boundary = createOption.none();

    if (body === null) {
      body = createOption.none();
    } else {
    }
  }
}
